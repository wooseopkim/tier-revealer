{
  "name": "@tier-revealer/lib",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "tsx --test ./**/*.test.*",
    "lint": "prettier --check . && eslint .",
    "format": "prettier --write . && eslint . --fix",
    "check": "tsc && :"
  },
  "devDependencies": {
    "tsx": "^4.11.2"
  }
}